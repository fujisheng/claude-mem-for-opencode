# Claude-Mem 上游集成配置

本目录包含 Claude-Mem 插件的自动安装和更新配置。

## 📁 文件说明

| 文件 | 用途 |
|------|------|
| `claude-mem-setup.yaml` | 完整的安装配置规格（机器可读） |
| `INSTALL.md` | 快速安装指南（人工阅读） |
| `AI-EXECUTION-GUIDE.md` | **AI 执行手册**（指导 AI 如何操作） |
| `install-upstream.cjs` | **自动安装脚本**（一键安装） |
| `update-upstream.cjs` | **自动更新脚本**（一键更新） |

## 🚀 快速开始

### 首次安装

**人工执行**:
```bash
cd .opencode/plugins/claude-mem/.opencode
node install-upstream.cjs
```

**AI 执行**:
AI 应该读取 `AI-EXECUTION-GUIDE.md` 并按流程执行。

### 更新上游

```bash
node update-upstream.cjs
```

## 📖 使用场景

### 场景 1: 全新环境安装

当用户在新环境中使用 claude-mem 插件时：

1. AI 检测到 `vendor/claude-mem/` 不存在
2. AI 执行 `install-upstream.js`
3. 自动完成：
   - 检查依赖
   - 克隆上游仓库
   - 安装依赖
   - 构建项目
   - 验证安装
4. 提醒用户重启 Opencode

### 场景 2: 定期更新

当上游有新版本时：

1. 用户说"更新上游"
2. AI 执行 `update-upstream.js`
3. 自动完成：
   - 拉取最新代码
   - 更新依赖
   - 重新构建
   - 验证更新
4. 提醒用户重启以生效

### 场景 3: 故障排除

当工具调用失败时：

1. AI 检查上游目录是否存在
2. 检查构建产物是否完整
3. 如有问题，重新安装
4. 验证修复

## 🔧 技术架构

```
.opencode/plugins/claude-mem/
├── .opencode/                    # ← 本目录：安装配置
│   ├── claude-mem-setup.yaml     # 完整配置
│   ├── INSTALL.md                # 安装指南
│   ├── AI-EXECUTION-GUIDE.md     # AI 手册
│   ├── install-upstream.js       # 安装脚本
│   └── update-upstream.js        # 更新脚本
├── src/                          # 本地适配代码
├── vendor/                       # ← 上游代码安装到这里
│   └── claude-mem/               # ← 上游仓库
└── ...
```

## 📝 配置说明

### 上游仓库信息

- **仓库**: https://github.com/thedotmack/claude-mem
- **分支**: main
- **版本**: 自动跟踪最新

### 依赖要求

- **Node.js**: >= 18.0.0
- **Bun**: >= 1.0.0（强烈推荐）
- **Git**: 任何版本

### 安装路径

上游代码安装在：
```
.opencode/plugins/claude-mem/vendor/claude-mem/
```

### 构建产物

构建后必须存在的文件：
- `vendor/claude-mem/plugin/scripts/worker-service.cjs`
- `vendor/claude-mem/plugin/scripts/worker-wrapper.cjs`
- `vendor/claude-mem/plugin/scripts/mcp-server.cjs`

## 🔄 更新策略

### 自动更新检查

可以定期（如每月）执行：
```bash
cd vendor/claude-mem
git fetch origin
git log HEAD..origin/main --oneline
```

### 更新流程

1. 执行 `update-upstream.js`
2. 检查是否有新版本
3. 自动拉取并构建
4. 提醒用户重启

## 🐛 故障排除

### 问题: 安装脚本失败

**解决方案**:
1. 检查 Node.js 版本: `node --version`
2. 检查 Git 是否安装: `git --version`
3. 检查网络连接
4. 手动执行步骤查看详细错误

### 问题: 构建失败

**解决方案**:
1. 删除 node_modules 重新安装
2. 检查是否有权限问题
3. 查看上游 issue: https://github.com/thedotmack/claude-mem/issues

### 问题: 工具调用失败

**解决方案**:
1. 检查上游目录是否存在
2. 检查构建产物是否完整
3. 重新运行安装脚本
4. 重启 Opencode

## 📚 相关文档

- **安装指南**: [INSTALL.md](./INSTALL.md)
- **AI 手册**: [AI-EXECUTION-GUIDE.md](./AI-EXECUTION-GUIDE.md)
- **配置规格**: [claude-mem-setup.yaml](./claude-mem-setup.yaml)
- **上游仓库**: https://github.com/thedotmack/claude-mem
- **上游文档**: https://docs.claude-mem.ai

## 🎯 设计目标

1. **自动化**: AI 可以完全自动完成安装和更新
2. **可靠性**: 自动检查依赖，失败时提供清晰的错误信息
3. **可维护性**: 配置和脚本分离，便于更新
4. **可追溯**: 保留版本信息，便于排查问题

## 🤝 贡献

如果需要修改安装流程：

1. 修改 `claude-mem-setup.yaml` 中的配置
2. 更新 `install-upstream.js` 和 `update-upstream.js` 脚本
3. 更新 `AI-EXECUTION-GUIDE.md` 中的流程
4. 测试安装和更新流程

## 📄 许可证

与上游 claude-mem 一致：AGPL-3.0
